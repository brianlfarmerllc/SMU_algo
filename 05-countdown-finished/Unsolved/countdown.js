// Write code to print all numbers from `num` down to 1
// You may assume `num` will be a positive number

// iterate i starting at num until greater than 0 console logging i
var countdown = function (num) {
  for (i = num; i > 0; i--) {
    console.log(i);
  }
};

// All Tests Passed when running test HTML
